# Release 0.3.1

## Decisions

Cut 0.3.1 on 2026-09-07 for leftover csv, json, and xml batched_export requests that are not JSON export_meta and not a matching-format batch. Those requests return 406.

## Effects

CHANGELOG.md and lib/activeadmin/batched_export/version.rb moved to 0.3.1. make release was not run. gem push still needs a clean tree and an explicit Continue.

## Next

make release when ready to tag and push the gem. Hosts that prepend a mismatch 406 can drop that prepend after upgrade.

## Source

usr/docs/changelogs/20260907141800_reject-mismatched-data-formats.md. usr/docs/issues/20260907141800_mismatched-export-format-406.md.
